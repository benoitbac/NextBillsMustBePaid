{
  "name": "overdraft",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "OVERDRAFT — jeu incremental actif : casser des tirelires pour payer ses factures.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "sim": "tsx tools/sim.ts"
  },
  "devDependencies": {
    "@types/node": "^26.4.1",
    "@types/three": "^0.185.4",
    "tsx": "^4.20.6",
    "typescript": "^5.9.3",
    "vite": "^7.1.14",
    "vitest": "^5.0.0"
  },
  "dependencies": {
    "three": "^0.181.0"
  }
}
